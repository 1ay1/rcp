[build-system]
requires = ["setuptools>=64", "pybind11>=2.12"]
build-backend = "setuptools.build_meta"

[project]
name = "rcp-protocol"
version = "1.0.0"
description = "Retrieval Context Protocol — Python bindings over the type-theoretic C++ SDK"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "RCP Working Group" }]
keywords = ["rcp", "rag", "retrieval", "protocol", "json-rpc", "mcp", "acp"]

[project.urls]
Homepage = "https://1ay1.github.io/rcp/"
Specification = "https://github.com/1ay1/rcp/blob/main/spec/rcp-1.0.md"
Source = "https://github.com/1ay1/rcp"
